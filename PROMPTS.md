# PROMPTS
记录我各个工作对话的PROMPTS，因为我自己不写代码，PROMPTS其实就是我的“代码”，包含了最凝练的规范要求等，也可能存在某些问题可以溯源。并且某些情况下可以预写一些prompts而不是等待agents跑完才写。
## byte pair encoding
---
我现在要开始逐步实现tokenizer了。计划是在我理解的基础上，每一步让agent帮我写具体功能（我来描述），整体还是vibe coding，但是我会详细check。首先第一步是写Byte pair encoding算法。也就是假设有一系列的unicode输入，转成UTF-8，然后转成一系列的int（种类是0\~255），需要在int数组每次找出现最多的pair（这里指的是2个int组队）成为一个新int比如256，逐次迭代，依次得到257,258等。这样会逐步扩大词汇表（token种类），缩减上下文长度。支持人工调整词汇表的大小（也就是调整迭代次数）。并且支持对于一个输入string，输出压缩后的token sequence的长度（以及内容，可选项）和压缩比等信息。

---

bpe代码的注释改成中文，稍微具体一点。然后告诉我目前这个测试代码是什么意思，我自己有没有比较直观一点的图形化的测试方式呢？

---

你这个图形交互视图特别不错。不过这是你codex内才能看的吧？有没有办法在浏览器看并且是基于当前的bpe代码？最好是和bpe代码本身尽可能解耦，也就是代码内不管图形化展示。图形化展示的代码或文档都集中一个位置，预定义一个风格，后续还可能有其他要展示的。最后告诉我在终端如何运行来在浏览器看图形化界面。

（结论：远程连接时最好是vscode终端去跑这种端口转发而不是直接在主机或codex，因为vscode有插件。）

关于byte pair encoding，我发现目前图形化界面里面，每一行都被称为一个“样本”，虽然是一起参与合并的。这个称呼规范吗？目前的图形化界面输出请检查一下正确性。

## git

能否做一下git的提交和推送，把规范文档和byte pair算法区分开提交并推送远程

## decode

前面的bpe本质就是分词器的encode阶段对吧【其实并非！】。现在我需要你实现decoder。输入是一个int列表，要返回python string。decode需要在encode【Tokenizer构建】之后发生，会需要bpe的词汇表信息。实现后做一些测试，然后类似bpe做一个前端可供演示，以及代码注释需要用中文。

一个bug：unicode并不是每个int都对应一个符号，因为支持扩展字节数，需要用前缀区分当前是几个字节，所以诸如128这种是不能作为首个int去decode成raw text的，这种情况decode一般需要输出特殊问号，error处理方式是“replace”。目前版本代码似乎是直接报错说“这些 token 都存在于词表中，但按顺序拼接后不是有效的 UTF-8。”，我希望改成replace方式，就是输出特殊问号。

## encode

前面的bpe，按我理解，应该是用于构建Tokenizer的一种简单的方式，其流程类似encode但是并不是一回事。encode是基于已经确定的Tokenizer，也就是已经完成了诸多轮的merge，有确定的词汇表。现在需要实现与前面的bpe流程配套的encoder。输入是string，然后要返回一个int列表。encode需要在Tokenizer构建之后发生，会需要bpe的词汇表信息。实现后做一些测试，然后类似bpe做一个前端可供演示，以及代码注释需要用中文。注意，要有一个正确的顺序，每次把两个int merge后的int还有可能触发后续的某些merge。我们bpe形成的词汇表是有顺序的（一个int只可能又2个更小的int merge得到），encode也必须遵守顺序。

## better

GPT2论文提到一个问题，BPE算法会把一些不该merge的merge，比如`dog.`, `dog?` ,`dog `等。怎么避免一些不应该发生的merge？现在希望优化分词器使得某个字符永远不应该参与merge。这里请你先参考gpt2的源码：https://github.com/openai/gpt-2/blob/master/src/encoder.py 其中展示了如何实现，核心是re.compile部分，可以参考。大意就是在encode之前把string按某种规则做一个spilt，然后每个独立string分别去做分词，再把得到的token sequence合并。现有的这个最简单的分词器代码，需要你存档一份，并保留对应的前端代码使得前端展示可以用。然后基于已有架构，去实现支持避免特殊merge的更好的分词器。不要照抄gpt2因为有一些问题，比如其中预设了`'s`,`'re`等英文常用的缩写，但是我们不需要这种预设，也不方便扩展。我也需要有对应的前端，并且前端需要能展示我们目前分词器做split的策略。目前也不需要有太复杂的策略，有一些最基础的就行。建议新建一个文件夹来存放代码（而不是放到项目根目录）并做好隔离，不同分词器放不同文件夹。你可以先定plan然后再逐步进行。

我检查了一下，原版的前端没问题，新版这个split-aware的前端感觉有问题，里面首先比例就不对，进去元素太多太大，浏览器一面放不下，如图所示，比如“Protected 是 barrier，不是 pair 黑名单”的内容在页面右边，需要拉动下方的bar才能看全。可能要重新组织一下，改一下前端，让逻辑更清晰一点，也不需要有太多冗长的介绍。整体设计思路没问题，先构建词汇表然后给示例去encode和decode。请你修改后再做git提交

非常好。可以做提交。另外把根目录的文档也提交了，提交前检查一下没什么问题就行。

## todo

?为什么还有未提交文件，不应该都明确了吗？另外根目录的bpe.py应该就是basic吧，我直接删了。请你重新梳理一下git。应该是需要都提交的

确认。因为是重复内容就不需要保留了。
